# 印库 · M1 结案报告

> **时间**：2026-09-11 ~ 2026-09-12  
> **阶段**：M1「止血」  
> **目标**：让这个项目**能给别人用**  
> **状态**：✅ 已结案

---

## 一、M1 到底是什么

打个比方：

> 你的项目是一栋房子。之前**水管在漏、有根电线没接上、还有一盏灯是坏的**。  
> 能住，但**别人不敢住**。
>
> M1 干的事就是**先把水电修好**——不是装修，是让它达到「能住人」的最低标准。

**怎么判断「到底完了没」？**

> **找个同学，把仓库地址发给他。他能不能自己跑起来？**  
> 能 → M1 完。不能 → M1 没完。
>
> （M1 的目标是「能给别人用」，所以**你自己说了不算，别人能跑起来才算**。）

---

## 二、M1 的三条主链

### 链 1：让它「认人」（2026-09-11 21:00）

**问题**：两个人一起用，答题记录会记到别人名下。

**怎么发现的**：你自己按 F12，看到错题本请求了一个**根本不存在的接口**（404）。

**做了什么**：

| 步骤 | 内容                                              |
| -- | ----------------------------------------------- |
| ①  | 补上缺失的 `/wrong` 接口                               |
| ②  | 发现 `records` 表**根本没有 `user_id` 这一列**——前端传了，后端没接 |
| ③  | 给表加 `user_id` 列                                 |
| ④  | 改 4 处代码，让后端真的接住这个身份                             |
| ⑤  | 查真实 uid、给旧数据补上归属                                |
| ⑥  | **你自己发现「薄弱项没变」** → 又改 3 处                       |

**结果**：12 条答题记录，条条归对人，不再串号。

---

### 链 2：让代码「真的在干活」（2026-09-11 23:15）

**问题 1**：题库有 **272 道题，其中 59 道永远抽不到**。  
原因是一行筛选条件写错了，把三类题型永久排除在外。

**问题 2**：有个建表的函数 `init_db()`，**写好了但从来没人调用**，而且一跑就报错（`UnboundLocalError`）。

**中间踩的坑（很重要）**：

> 你把 SQL 改成 `SELECT * FROM questions`，觉得「**大概率没问题**」。  
> 结果一跑，带章节参数时直接**语法错误**——因为拼出来变成了  
> `SELECT * FROM questions AND chapter=?`。
>
> 正解是 `WHERE 1=1`：一个永远为真的占位，**保住后面 AND 能接着拼**。

**结果**：272 道题全能抽到（实测「不带章节: 272 / 带章节: 23」）；服务启动时自动建表，并打印 `[启动自检] 数据库表已就绪`。

---

### 链 3：让它「会说话 + 被别人看见」（2026-09-12 13:24）

**做了三件事**：

**① 每个请求花多少毫秒，记下来**  
实测立刻抓到：`/rag/search` 冷启动要 **15.2 秒**，热态 0.8~~1.3 秒——而其他接口只要 6~~7 毫秒。

**② 崩了自动记日志（带完整堆栈）**  
原来未捕获的异常被**静默吞掉**，什么痕迹都不留。  
补上之后，用 `POST /check` 传一个错误参数，立刻抓到 `practice.py:34` 的真凶。

> 顺便学会了一个技能：**看堆栈时，忽略 `venv\Lib\site-packages\` 的行，只看带 `印库\app\` 的行。**

**③ 代码传到 GitHub，每次改动自动检查**  
第一次跑就**红了**——80 个代码规范问题。修掉 20 个自动可修的，剩下 60 个（45 个「行太长」+ 15 个「一行多语句」）。  
最后**关掉这两条规则**而不是硬改（长 SQL 和长 prompt 拆不开，硬拆更难读）。

> 记住这个态度：**工具是给你用的，不是铁律。**

---

## 三、M1 收尾：体检发现的 5 个尾巴（2026-09-12）

功能都做完了，但一检查「能不能给别人用」，发现还有 5 个坑：

| # | 问题                                                 | 修法                                       |
| - | -------------------------------------------------- | ---------------------------------------- |
| ① | `/check` 传错参数**服务器直接崩**（500），应该是 400               | 加一句类型检查：先问「你是不是整数」，再比大小                  |
| ② | **1.6 MB 的二进制向量库**被传进了 git，每次跑都变                   | `git rm --cached` + 加进 `.gitignore`      |
| ③ | `build_index.py` 里**写死了本机路径** `C:\Users\36085\...` | 改成从 `app/config.py` 读                    |
| ④ | 别人 clone 下来**不知道怎么装、怎么跑**                          | 新建 `.env.example` + readme 补全（30 → 65 行） |
| ⑤ | 测试没有接进自动检查                                         | 接进 CI，Ollama 相关用例在云端自动跳过                 |

**第 ③ 个坑的因果链特别值得记**：

> 我们刚把「向量库」从仓库移出去  
> ↓  
> 别人就必须自己跑 `build_index.py` 重新生成  
> ↓  
> **但这个脚本跑不了**——路径写死了，别人电脑上没这个文件夹

**一句话：移走一个东西，会暴露出别处藏着的依赖。**

---

## 四、数字总结

| 项目     | 数字                                     |
| ------ | -------------------------------------- |
| 提交次数   | **9 次**                                |
| 改动文件   | **31 个**                               |
| 代码增删   | **+373 行 / −2018 行**（删的比加的多，因为清掉了垃圾文件） |
| 仓库文件数  | 54 → **51 个**                          |
| 线上自动检查 | **3 次红 → 1 次绿**                        |

---

## 五、M1 最值钱的不是代码，是这 4 个坑

### 坑 1：「觉得没问题」≠「真的没问题」

你把 SQL 改完觉得「大概率没问题」，一跑就语法错误。

### 坑 2：改一处，可能牵出另一处

移走向量库 → 暴露 `build_index.py` 写死路径。  
**代码是有连锁反应的。**

### 坑 3：本地绿 ≠ 线上绿

本地测试全过，推上去还是红。因为**本地只跑了 1 项，云端跑的是全部**。

三次红的原因分别是：

| 第几次   | 挂在哪    | 原因                                   |
| ----- | ------ | ------------------------------------ |
| 第 1 次 | 代码规范检查 | `TestClient` 被 import 了两次            |
| 第 2 次 | 装依赖    | 云端 Python 是 3.11，你电脑是 3.13，**版本对不上** |
| 第 3 次 | —      | **全绿 ✅（31 秒跑完）**                     |

### 坑 4：「任务做完」≠「目标达成」

我说过「M1 完成了」，用的是「清单都打勾了」的标准。  
一体检才发现，**目标（能给别人用）还没达成**。

> 就像考试交卷：**卷子写满了 ≠ 全对。**

---

## 六、M2 是干什么的

### 6.1 一句话

> **M1 是「让它能跑」，M2 是「搞清楚它跑得准不准」。**

打个比方：

> 你刚把一把枪修好了，能打响。  
> 但你**没打过靶**——不知道它准不准，偏左还是偏右。
>
> M2 就是：**立个靶子，打十发，数一数中了几发。**

### 6.2 为什么现在做

因为 M1 给了你一个**可证明的载体**：

- 有了自动检查 → 你的改动**不会偷偷弄坏别的东西**
- 有了耗时日志 → 优化有没有效果**看得见**

**没有这两样，M2 的改进就是自说自话。**

而且后面 M3（换算法）**必须先有 M2**——没有尺子就动核心算法，等于蒙着眼睛改。

### 6.3 四件事

**① 造尺子**  
20~30 条「学生会怎么问 → 正确答案是哪道题」。  
例：`"快排为什么不稳定"` → 应该是第 8 章那道稳定性题。

**② 量一次现状**  
拿这 20 条去跑现在的搜题功能，**数中几条**。

**③ 改，再量一次**  
调检索参数，重跑。**数字涨了，才算改对了。** 没涨就是白改。

**④ 定个约定**  
先写清楚「输入什么、输出什么」，再写代码。

### 6.4 做完你手上会多出什么

**一张分数表**：

> 优化前 12/20 → 优化后 16/20

**一句能写进简历、面试能说的话**：

> 「我把搜题的命中率从 60% 提到了 80%。」

**这句话的分量，比「我做了一个 AI 刷题系统」重得多。**  
因为前者是**结果**，后者只是**描述**。面试官只信结果。

---

## 七、下一步

M2 的第一件事：**给搜题功能立靶子**。

具体就是：你想 20~30 条「学生真的会这么问」的句子。

---

*报告生成于 2026-09-12*
