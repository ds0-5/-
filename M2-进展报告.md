# 印库 · M2 进展报告

> 时间：2026-09-12（下午 13:30 ~ 16:15）
> 阶段：M2「评测」
> 状态：⏳ 进行中（做完约 90%）

---

## 一、一句话总结

> **给你的「搜题」功能做了一次体检 + 治病，让它从「20 道题对 16 道」变成「对 19 道」。**

---

## 二、事情的起因

**M2 原本要干的事**：给功能打分，用数字证明「我改进了」。

**中途发现的问题**：

你项目里有个「搜题」功能（后端有接口），但：

- **前端没有任何页面调用它** → 你从来没见过
- 它原本的用途，是**给 AI 讲解找参考资料**（而不是给学生当搜索引擎）

**所以 M2 的路线调整成**：

> **先把「找资料」这件事做准，再接进 AI 讲解。**

---

## 三、一步步怎么走的

### 第 1 步：出考卷

20 条「学生怎么问 → 应该搜到哪个知识点」。

**这 20 条是你自己写的**（我只给了格式和 3 条示范），你写得比我给的版本更口语。

### 第 2 步：写阅卷程序

一个小脚本，自动跑、自动比对、自动算分。

### 第 3 步：第一次考试

```
16 / 20
```

### 第 4 步：发现「判分太严」

有 2 道其实**搜对了**：

| 期望 | 实际搜到 | 判定 |
|---|---|---|
| 折半条件 | 折半查找条件 | **是一个意思，被误判** |
| 完全树高度 | 完全树最少结点 | **同一主题，被误判** |

**改了判分规则**（分「完全命中」和「大致命中」两级）→ **真错的只剩 2 道**。

### 第 5 步：修第 1 个毛病 —— 中文分词

**问题**：原来是「一个字一个字」拆句子，太容易误伤。

> 「**广**度优先」里的「广」字，撞上了「**广**义表**深**度」→ 三个字全中 → 把正确答案挤掉。

**改法**：改成「**一个字 + 两个字**」都拆。

**结果：18 / 20**

### 第 6 步：修第 2 个毛病 —— 换模型

**问题**：原来用的向量模型是**英文的**，中文理解很差。

> 它把「深度优先和广度优先有啥区别」的前 12 个结果**全给了「广义表」**。

**改法**：换成 `bge-m3`（智源出的中文模型）。

**结果：19 / 20** ✅ 第 15 条修好了

### 第 7 步：治本

**问题**：模型名字写在**两个文件**里，改一个忘一个。

（这一步你就踩了：改了 A 忘了 B → 索引和检索对不上 → 报错）

**改法**：让其中一个文件不用自己写，直接去另一个文件「借」。

---

## 四、成果

### 分数变化

| 阶段 | 完全命中 |
|---|---|
| 一开始 | 16 / 20 |
| 改分词后 | 18 / 20 |
| 换模型后 | **19 / 20** |

### 一句能写进简历的话

> **「通过评测驱动的方式，把检索命中率从 80% 提升到 95%。」**

**注意**：这句话比「我做了一个 AI 刷题系统」重得多。

- 前者是**结果**（有数字、可验证）
- 后者只是**描述**

---

## 五、踩过的坑（这才是最值钱的部分）

### 坑 1：改一个地方，修好 3 个，弄坏 2 个

**改分词那次**：

- 修好了「头结点」「完全树高度」「深度优先」
- **却弄坏了「栈队列」「堆排序」**

**如果没有那把尺子，你只会看到「修好了 3 个」，然后高高兴兴以为改对了。**

> **这就是「先造尺子，再改东西」的原因。**

### 坑 2：判分标准本身也会错

第一次跑完，有 4 道「没中」。

但仔细一看——**有 2 道其实是搜对了，是我的判分规则太死板。**

> **评测的第一个坑，往往不是「系统不准」，而是「你的尺子不准」。**

### 坑 3：同一个名字写在两个地方

模型名写在 `rag_service.py` 和 `build_index.py` 两处。

改了其中一个，忘了另一个 → 索引 768 维、检索 1024 维 → **报错、白跑一遍**。

> **治本 = 只留一份，另一份直接来「借」。**

### 坑 4：中文没有空格

英文 `hello world` 天然是两个词。

中文「今天天气不错」**没有空格**——该切成「今天 / 天气 / 不错」，还是「今 / 天 / 天 / 气 / 不 / 错」？

**这是中文搜索特有的难题。**

---

## 六、下一步

### M2 还剩最后一步：把检索接进 AI 讲解

```
学生答完题
     ↓
AI 拿到的资料：整章笔记
     ↓
再检索出 3 道相似题        ← 这一步还没接
     ↓
AI 讲这道题 + 顺便说「再练这 3 道」
```

**具体位置**：`app/api/routers/ai.py` 的 `/explain` 接口。

### 之后还有

- **M3 算法**：把现在的复习算法换成「知识类型 × 考点权重」二维 SRS —— **简历里唯一的原创点**
- **M4 上线**：Docker、多用户隔离

---

## 附：今天的所有提交

```
ab7ef30  refactor: build_index 从 rag_service 读取模型名，避免两处不一致
86456d5  feat: 换用 bge-m3 中文向量模型，检索命中率 90% -> 95%
cc4c2fd  perf: 中文分词改为单字+双字，检索命中率 80% -> 90%
```

---

*报告生成于 2026-09-12 16:17*
